const propertyFormatter = require('../js/property-formatter');
const chai = require('chai');
const expect = chai.expect;

describe('propertyFormatter', function () {
  it('formats property inside element into markdown');
});
